ジャンル新規登録

<%= form_with model:@genre, url: admin_genre_path(@genre) do |f| %>
	<%= render 'layouts/error_messages', resource: @genre %>
	<%= f.label :name, "ジャンル名:" %>
	<%= f.text_field :name %>

	<%= f.submit '更新する' %>
<% end %>